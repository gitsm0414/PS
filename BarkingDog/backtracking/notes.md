<백트래킹>
현재상태에서 가능한 모든 후보군을 따라 들어가며 탐색하는 알고리즘

nm9.cpp 참고사항
구분                   전역 함수(기존 방식)                        람다 함수 (std::function)

변수 접근               N, M, isused 등을 전역 변수로 빼야 함       main 함수 안의 지역 변수를 그대로 사용 가능
캡처 ([&])             불가능 (인자로 다 넘겨줘야 함)               가능 (바깥 변수를 내 것처럼 씀)
코드 위치               main 함수 밖에 따로 존재                   main 함수 내부, 실제 사용되는 곳 옆에 존재
재귀 호출               함수 이름으로 바로 가능                     std::function으로 감싸야 가능